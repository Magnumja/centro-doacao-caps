export type Cap = {
  id: string
  title: string
  description?: string
}

export type Need = {
  id: string
  title: string
  amount: number
}

export type User = {
  id: string
  name: string
  email?: string
}

// Nota: este arquivo contém apenas tipos compartilhados.
